// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;




/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  experimental: {
    appDir: false, // Disable App Router if needed
  },
};
 export default nextConfig;
  
  // module.exports = nextConfig;
  